package gamelogictests;

import static org.junit.Assert.*;
import gamelogic.*;

import org.junit.Test;

public class GameTest
{
	
	@Test
	public void testGameCreation()
	{
		Game game = new Game();
		/*
		assertTrue(game.getBoardA() instanceof Board);
		assertTrue(game.getBoardB() instanceof Board);
		assertTrue(game.getPlayerA() instanceof Player);
		assertTrue(game.getPlayerB() instanceof Player);*/
		
	}
	
}
